<%- include('partials/header/world.ejs') %>
    <div class="block">
        <div class="underline">
          <h1>MY FAVORITE</h1>
        </div>
        <% if(item.length == 0) { %>
        <div>
            <h3 style="text-align: center; margin-top:2rem;">No item in favorite</h3>
        </div>
        <% } else { %>
            <div class="row warp">
                <div id='table'>
                    <% item.forEach((item) => { %>
                    <% var avgRating; %>
                    <% if(item.comments.length == 0){ %>
                    <%  avgRating = 0; %>                                                  
                    <% }else{ var i = 0,j= item.comments.length; %>
                        <% item.comments.forEach(function(comment){ %>
                        <% if(typeof(comment.rating) == 'undefined') { %>
                        <% j-- %>
                        <% } else { %>
                            <% i = i + comment.rating %>
                        <% } %>
                        <% }); %>
                        <% avgRating = (i/j).toFixed(1) %>
                    <% } %>
                    <% if(isNaN(avgRating) || avgRating == 0){ %>
                    <% avgRating=0 %>
                    <% } %>
                            <div class="card">
                                <img src='<%= item.image %>' class='img'>
                                <% var txtRate; %>
                                <% if(avgRating == 0){ %>
                                  <% txtRate='No one rate this product' %>
                                  <% } else { %>
                                  <% txtRate = avgRating + ' / 5.0' %>
                                  <% } %>
                                <div class="row iconP">
                                    <form action='/user/favorite/remove/<%= item.id %>' method='POST'>
                                      <button class='heart'><i class="fas fa-heart"></i></button>
                                    </form>
                                    <i class="fas fa-star" style='color: orange; display: inline;' title='<%= txtRate %>'></i>
                                </div>
                                <div class="detail">
                                  <h3><%= item.name %></h3>
                                  <p>Sold : <%= item.sold %></p>
                                  <h4>฿<%= item.price %></h4>
                                  <div class="row">
                                      <span>By :&nbsp;</span>
                                      <div>
                                        <a href='/seller/shop/<%= item.author.id%>'>
                                        <% if(typeof(item.author.image) == 'undefined') { %>
                                          <i class="fas fa-user me-3"></i><%= item.author.username %>
                                        <% } else { %>
                                          <img src='<%= item.author.image %>'><%= item.author.username %>
                                        <% } %>
                                        </a>
                                      </div>
                                    </div>
                                    <div class="row" style="justify-content: space-between;">
                                      <a href ='/show/<%= item._id%>' style="color:blue;">View</a>
                                      <form action="/cart/<%= item._id %>" method="post">
                                        <button type="submit">Add to cart</button>
                                      </form>
                                    </div>
                              </div>
                              </div>
                    <% }); %>
                </div>
            </div>
        <% } %>
  </div>
  <%- include('partials/footer/Main2.ejs') %>